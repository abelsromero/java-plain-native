pluginManagement {
  plugins {
    id 'org.graalvm.buildtools.native' version '0.9.28'
  }
  repositories {
    mavenCentral()
    gradlePluginPortal()
  }
}

rootProject.name = 'java-plain-native'

include \
  'app',
  'base',
  'configuration',
  'logging-api',
  'logging-slf4j',
  'test-tools'
