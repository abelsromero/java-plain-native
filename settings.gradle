pluginManagement {
  plugins {
    id 'org.graalvm.buildtools.native' version '0.9.27'
  }
  repositories {
    mavenCentral()
    gradlePluginPortal()
  }
}

rootProject.name = 'java-plain-native'

include \
  'app',
  'base',
  'configuration',
  'test-tools'
